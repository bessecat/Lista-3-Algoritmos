#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n, soma;

    for(int i=0; n>=0; i++){
        printf("Digite um numero:\n");
        scanf("%d",&n);
        soma=soma+n;
    }
    if(n<0){
        printf("A soma e: %d",soma);
        }

    return 0;
}
